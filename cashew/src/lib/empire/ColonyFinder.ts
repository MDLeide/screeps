import { Empire } from "./Empire";
import { Colony } from "../colony/Colony";

/** Finds new colonies and adds them to the empire. */
export class ColonyFinder {

    /** Finds new colonies and adds them to the empire. */
    public static searchForNew(empire: Empire): Colony[] {
        
    }
}
